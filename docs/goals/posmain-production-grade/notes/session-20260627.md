# Goal Maker board — POSMAIN Production Grade

Run:

```text
/goal Follow docs/goals/posmain-production-grade/goal.md
```

## Session receipts (2026-06-27)

### Gate 1 — DONE (deployed Hetzner)
- **P01** `api/health.php`: cloud role skips branch UUID requirement → public health **200**
- **P02** `do/recost.php`, `js/ajax/recost.php`: live guard via `InventoryLegacyStockEndpointGuard`
- Contract tests: `health_endpoint_contract_test.php`, `recost_live_guard_contract_test.php`

### Gate 2 — IN PROGRESS
- **Migration fix**: operational store scope + migration idempotency lookup (`InventoryHistoricalMigrationService`)
- **Backfill pagination**: `--min-fat-detail-id` on `inventory_backfill_from_fat_details.php`
- **posmain_shop2**: backfill +32 movements, rebuild 63 balances; reconciliation 35 unhandled; accounting 100 missing journals
- **focushouse**: full backfill ~13k rows; mirror repair 233 items; reconciliation check down to 5 unaccepted
- **kody2**: cutover + production readiness green except browser QA evidence

### Gate 3 — QUEUED
- Production profile resolver, write-path map line-proof drift, Playwright/hosted QA campaign, browser evidence automation

## Hetzner verification

```bash
curl -sS https://erp.withmoova.com/api/health.php   # ok: true, HTTP 200
POSMAIN_DB_NAME=kody2 php tools/inventory_cutover_readiness.php          # READY
POSMAIN_DB_NAME=posmain_shop2 php tools/inventory_cutover_readiness.php  # BLOCKED reconciliation+accounting
POSMAIN_DB_NAME=focushouse php tools/inventory_cutover_readiness.php     # BLOCKED reconciliation
```

## Uncommitted local changes

Gate 1 + migration pagination + write_path_map entries — deployed via SCP; commit when ready.
